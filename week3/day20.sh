#!/bin/bash

# SUBPROCESS
  The subprocess module runs external programs from Python code and captures their output.
  We can drive Nmap, ffuf, sqlmap, or any command-line utility from Python, capture its output, parse it, and take automated action based on the results.
  This is the foundation of security automation.

  # This concept is practiced in python file: subprocess_masterclass.py